// Converted to JS-friendly preview to avoid TS loader issues in Storybook Webpack
import React from 'react';

const preview = {
  decorators: [
    (Story) => <Story />,
  ],
};

export default preview;


